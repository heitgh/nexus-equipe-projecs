'use client';import Link from 'next/link';import {usePathname} from 'next/navigation';import {modules} from '@/lib/modules';export function PortalNav({role}:{role:string}){const path=usePathname();return <nav aria-label="Portal acadêmico"><Link className={path==='/portal'?'active':''} href="/portal">Início</Link>{Object.entries(modules).filter(([,m])=>m.roles.includes(role)).map(([key,m])=><Link className={path==='/portal/'+key?'active':''} href={'/portal/'+key} key={key}>{m.title}</Link>)}{['admin','director'].includes(role)&&<Link href="/portal/equipe">Criar acesso</Link>}<Link href="/">Site institucional ↗</Link></nav>}
