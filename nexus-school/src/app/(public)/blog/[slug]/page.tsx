import {notFound} from 'next/navigation';import Link from 'next/link';import {publishedArticles} from '@/lib/public-data';export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const articles=await publishedArticles();const a=articles.find(a=>a.slug===slug);if(!a)notFound();return <article className="container section prose"><Link href="/blog" className="text-link">← Voltar às conexões</Link><span className="eyebrow spaced">{a.tag} · Conteúdo demonstrativo</span><h1 className="page-title">{a.title}</h1><p className="lead">{a.text}</p><p>Na proposta da Nexus School, aprender é participar. A integração entre cursos incentiva a troca de perspectivas e o desenvolvimento de soluções em equipe.</p><p>Atividades práticas, espaços de colaboração e acompanhamento dos professores ajudam a conectar o conhecimento às possibilidades de carreira.</p><div className="notice">Esta publicação faz parte de um projeto educacional fictício. Programações e experiências descritas são demonstrativas.</div><Link className="button navy" href="/cursos">Encontre sua próxima conexão ↗</Link></article>}
