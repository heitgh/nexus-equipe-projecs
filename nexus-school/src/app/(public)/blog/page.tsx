import Link from 'next/link';import {publishedArticles} from '@/lib/public-data';export const dynamic='force-dynamic';export const metadata={title:'Conexões — Blog e mural'};export default async function Page(){const articles=await publishedArticles();return <div className="container section"><span className="eyebrow">BLOG · MURAL · EVENTOS</span><h1 className="page-title">Conexões que<br/><em>movem a Nexus.</em></h1><p className="lead">Notícias, ideias e experiências de uma comunidade em construção.</p><div className="news-grid">{articles.map((a,i)=><Link className="news-card" key={a.slug} href={'/blog/'+a.slug}><div className={'news-art news-'+i}><span>{a.tag.toUpperCase()}</span></div><span className="eyebrow">{a.tag} · Conteúdo demonstrativo</span><h2>{a.title}</h2><p>{a.text}</p><span className="text-link">Continuar lendo ↗</span></Link>)}</div></div>}
