import {publishedCourses} from '@/lib/public-data';import {CourseFilter} from '@/components/course-filter';export const metadata={title:'Cursos e graduações'};export default async function Page({searchParams}:{searchParams:Promise<{area?:string}>}){const{area}=await searchParams;const courses=await publishedCourses();return <div className="container section"><span className="eyebrow">ESCOLHA SEU PRÓXIMO PASSO</span><h1 className="page-title">Conhecimento para<br/><em>todos os caminhos.</em></h1><p className="lead">Explore nossas formações e encontre uma nova possibilidade.</p><CourseFilter initialArea={area} courses={courses}/></div>}
