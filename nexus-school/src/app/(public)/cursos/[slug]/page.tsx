import {notFound} from 'next/navigation';import Link from 'next/link';import {publishedCourses} from '@/lib/public-data';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const courses=await publishedCourses();return {title:courses.find(c=>c.slug===slug)?.name||'Curso'}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const courses=await publishedCourses();const c=courses.find(c=>c.slug===slug);if(!c)notFound();return <div className="container section"><Link href="/cursos" className="text-link">← Todos os cursos</Link><span className="eyebrow spaced">{c.category} · {c.area}</span><h1 className="page-title">{c.name}</h1><p className="lead">{c.description}</p><div className="detail-grid"><section className="panel"><h2>O que você vai explorar</h2>{c.topics.map((t,i)=><div className="topic" key={t}><span>0{i+1}</span><h3>{t}</h3></div>)}<h2>Aprender fazendo</h2><p>Projetos, prática orientada e colaboração acompanham sua jornada. A oferta de turmas, horários e modalidade será definida pela gestão.</p></section><aside className="panel"><span className="eyebrow">SUA PRÓXIMA CONEXÃO</span><h2>{c.duration}</h2><p>Presencial, online ou híbrido, conforme a turma.</p><p>Unidade principal: Jacareí–SP.</p><p>Valores e vagas: a definir na oferta demonstrativa.</p><Link className="button navy" href={'/inscricao?curso='+c.slug}>Solicitar inscrição ↗</Link><Link className="text-link spaced" href="/bolsas">Conheça as bolsas</Link><small>Oferta acadêmica fictícia para apresentação.</small></aside></div></div>}
