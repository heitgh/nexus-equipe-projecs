import {notFound} from 'next/navigation';import {approved} from '@/lib/auth';import {AccountForm} from '@/components/account-form';export default async function Page(){const{profile}=await approved();if(!['admin','director'].includes(profile.role))notFound();return <section className="panel"><span className="eyebrow">GESTÃO DE ACESSOS</span><h2>Novas conexões na equipe.</h2><AccountForm director={profile.role==='director'}/></section>}
